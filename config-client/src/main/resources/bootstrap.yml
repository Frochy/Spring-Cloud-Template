spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
server:
  port: 8881

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/